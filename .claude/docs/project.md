# Project Info

## 엔진 환경

| 항목 | 값                                   |
|---|-------------------------------------|
| 엔진 | Godot 4.7                           |
| 언어 | C# (.NET), GDScript                 |

프로젝트 설정 파일: `test/project.godot`

## 목적

게임잼 협업 대비 프로젝트.  
Claude를 활용해 코드 작성·문서 관리·팀 협업을 지원한다.

## 문서 관리 규칙

- 문서 동기화는 git 기록 기준으로 진행한다 (`docs(sync):` 커밋 마커).
- 동기화 완료 후 반드시 `docs(sync): <설명>` 커밋을 생성한다.